# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status -c ~/.config/i3status/i3status.conf

	colors{
		background #0D1C29
                urgent_workspace #0D1C29 #0D1C29 #FFFFFF
	}
}
